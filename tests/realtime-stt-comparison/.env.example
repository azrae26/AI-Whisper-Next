# OpenAI API Key (也可以直接在瀏覽器輸入)
# OPENAI_API_KEY=sk-...

# Google Cloud 認證
# 方式 1: 設定 service account JSON 路徑
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
#
# 方式 2: 使用 gcloud CLI 認證
# 執行: gcloud auth application-default login

# Server port (預設: 3000)
PORT=3000
